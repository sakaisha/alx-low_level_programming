#include "main.h"

/**
 * main - Entry point
 *
 * Description: Calls the print_square function to print squares of
 * different sizes.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_square(2);
	print_square(10);
	print_square(0);

	return (0);
}
