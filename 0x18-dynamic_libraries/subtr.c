#include "operations.h"

/**
 * subtr - Subtracts two numbers
 * @a: The first operand
 * @b: The second operand
 *
 * Return: The result of the subtraction
 */

size_t subtr(size_t a, size_t b)
{
	size_t result = 0;

	result = a - b;
	return (result);
}
